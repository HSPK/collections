{
  "name": "odd-index",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "games:serve": "node --experimental-strip-types scripts/play-games.ts",
    "new:project": "node scripts/new-project.mjs",
    "test": "playwright test",
    "test:gateway": "SITE_URL=http://127.0.0.1:1 playwright test tests/agent-gateway.spec.ts",
    "test:update-previews": "UPDATE_PREVIEWS=1 playwright test tests/previews.spec.ts"
  },
  "dependencies": {
    "astronomy-engine": "2.1.19",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.18.0",
    "@types/three": "^0.180.0",
    "typescript": "~5.9.2",
    "vite": "^7.1.5"
  }
}
